apiVersion: v1
kind: Service
metadata:
  name: {{ include "sre-test-development.fullname" . }}
  labels:
    {{- include "sre-test-development.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sre-test-development.selectorLabels" . | nindent 4 }}
